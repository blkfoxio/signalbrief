[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate --noinput && gunicorn signalbrief.asgi:application -k uvicorn.workers.UvicornWorker -w 2 -b 0.0.0.0:$PORT"
